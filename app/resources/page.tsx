import type { Metadata } from "next";
import PageShell from "@/components/PageShell";
import ResourceCapture from "@/components/ResourceCapture";
import Collapsible from "@/components/Collapsible";
import Reveal from "@/components/Reveal";
import { BookOpen, ClipboardList, Globe2 } from "lucide-react";
export const metadata: Metadata = { title: "Resources", description: "Guides, assessments, toolkits, templates, and implementation resources for established small and growing businesses moving from sustainability intention to practical implementation." };

const toolkits = [
[BookOpen, "The Sustainability Implementation Codex", "Flagship", "A practical implementation system for established small and growing businesses ready to build the systems, accountability, and measurement needed for credible sustainability progress.", "$179", "https://payhip.com/b/melRA", "Buy the Codex"],
[ClipboardList, "The Sustainability Reporting Playbook & Template", "Flagship", "A jurisdiction-by-jurisdiction guide to building sustainability reporting worth having, paired with a fillable template and a worked example.", null, null, null],
[Globe2, "The Global Sustainability Disclosure Landscape & Crosswalk", "Free guide", "A reference guide to the major disclosure frameworks by classification and jurisdiction, paired with a crosswalk mapping where their requirements overlap. Includes our Framework Reference Cards as a bonus.", null, null, null],
] as const;

export default function Resources(){return <PageShell eyebrow="Knowledge Suite" title="Guides, assessments, toolkits, templates, and implementation resources built for established small and growing businesses moving from strategy to execution."><Reveal><section className="section resource-section"><div className="container"><div className="resource-card"><div><h3>The Implementation Gap Assessment</h3><p>A practical 20-page self-assessment and fillable workbook for established small and growing businesses ready to move sustainability goals into action.</p><Collapsible><p>Includes a gap tracker, a prioritization scorecard, and a 30-day action plan, built around our Implementation Gap methodology.</p></Collapsible></div><ResourceCapture/></div></div></section></Reveal><Reveal><section className="section section-soft"><div className="container"><div className="section-head"><div><div className="eyebrow">Knowledge Suite</div><h2>Toolkits and templates for the work beyond the first assessment</h2></div></div><div className="grid-3">{toolkits.map(([Icon,title,tag,desc,price,url,cta])=>{const I=Icon as typeof BookOpen;return <article className="card" key={String(title)}><div className="icon-badge"><I size={22}/></div><h3>{String(title)}</h3><span style={{display:"block",fontSize:13,fontWeight:500,color:"var(--terracotta)",marginTop:4,marginBottom:10}}>{String(tag)}</span><p>{String(desc)}</p>{url?<p style={{marginTop:14}}><span className="resource-badge">{String(price)}</span><br/><a className="btn btn-secondary" href={String(url)} target="_blank" rel="noopener noreferrer">{String(cta)}</a></p>:<p style={{marginTop:14}}><span className="resource-badge">Coming Soon</span></p>}</article>})}</div></div></section></Reveal></PageShell>}
